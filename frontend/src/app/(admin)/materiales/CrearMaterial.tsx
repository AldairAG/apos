import CrearMaterialScreen from "@/features/material/presentation/screens/CrearMaterialScreen";

// Ruta: /materiales/CrearMaterial — modal de creación de material.
export default function CrearMaterial() {
  return <CrearMaterialScreen />;
}

import PanelSucursalScreen from "@/features/sucursal/presentation/screens/PanelSucursalScreen";

// Ruta: /sucursal/[sucursalId] — dashboard principal de la sucursal seleccionada.
export default function SucursalDashboard() {
  return <PanelSucursalScreen />;
}
